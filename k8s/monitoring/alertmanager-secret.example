# Create this secret with your Slack webhook URL:
# kubectl create secret generic alertmanager-slack -n monitoring \
#   --from-literal=webhook-url='YOUR_SLACK_WEBHOOK_URL'
#
# Then replace SLACK_WEBHOOK_URL in alertmanager-configmap.yaml with your URL
# and apply: kubectl apply -f alertmanager-configmap.yaml
#
# Or use: kubectl create secret generic alertmanager-slack -n monitoring --from-literal=webhook-url='YOUR_URL'
